syntax = "proto3";
option java_package = "core";

import "timer.proto";
import "database.proto";

message Message {
    oneof data {
        Delay delay = 1;

        // Database
        UpdateDatabase updateDatabase = 2;
        Zones zones = 3;
        ZonesResponse zonesResponse = 4;
    }
}